---
title: "Advanced MDX Features and Interactive Components"
date: "2024-01-20"
excerpt: "Explore the full power of MDX with interactive components, table of contents, and author information."
author: "jane-smith"
tags: ["mdx", "react", "interactive", "advanced"]
published: true
enableComments: true
showTableOfContents: true
coverImage: "/images/advanced-mdx-cover.jpg"
---

# Advanced MDX Features

This post demonstrates all the advanced features available in our MDX setup.

## Table of Contents

Since `showTableOfContents` is enabled in the frontmatter, you'll see a table of contents automatically generated by the rehype-toc plugin.

{/* toc */}

## Interactive Components

### Counter Component

Here's an interactive counter that maintains state:

<Counter initialValue={10} step={5} min={0} max={100} />

You can customize the counter with different props:

<Counter initialValue={0} step={1} min={-10} max={10} />

## Media Embeds

### YouTube Videos

<YouTube id="dQw4w9WgXcQ" title="Never Gonna Give You Up" />

You can also start videos at a specific time:

<YouTube
  id="dQw4w9WgXcQ"
  start={43}
  title="Rick Roll - Starting at 43 seconds"
/>

### Tweet Embeds

<Tweet id="1234567890123456789" />

## Enhanced Callouts

<Callout type="info" title="Information">
  This is an info callout with a custom title. Great for providing additional
  context or explanations.
</Callout>

<Callout type="warning" title="Important Warning">
  This is a warning callout. Use this to highlight potential issues or important
  considerations.
</Callout>

<Callout type="error" title="Error Alert">
  This is an error callout. Perfect for highlighting critical issues or mistakes
  to avoid.
</Callout>

<Callout type="success" title="Success Story">
  This is a success callout. Great for celebrating achievements or highlighting
  positive outcomes.
</Callout>

## Advanced Code Examples

### With Filename and Line Numbers

```typescript filename="advanced-example.ts" showLineNumbers
interface User {
  id: string;
  name: string;
  email: string;
  createdAt: Date;
}

class UserService {
  private users: User[] = [];

  async createUser(userData: Omit<User, "id" | "createdAt">): Promise<User> {
    const user: User = {
      id: crypto.randomUUID(),
      createdAt: new Date(),
      ...userData,
    };

    this.users.push(user);
    return user;
  }

  async getUserById(id: string): Promise<User | null> {
    return this.users.find((user) => user.id === id) || null;
  }

  async getAllUsers(): Promise<User[]> {
    return [...this.users];
  }
}

export { UserService, type User };
```

### Multiple Language Examples

Here's the same function in different languages:

```python filename="example.py"
def fibonacci(n):
    """Calculate the nth Fibonacci number."""
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# Example usage
for i in range(10):
    print(f"F({i}) = {fibonacci(i)}")
```

```rust filename="example.rs"
fn fibonacci(n: u32) -> u32 {
    match n {
        0 => 0,
        1 => 1,
        _ => fibonacci(n - 1) + fibonacci(n - 2),
    }
}

fn main() {
    for i in 0..10 {
        println!("F({}) = {}", i, fibonacci(i));
    }
}
```

```go filename="example.go"
package main

import "fmt"

func fibonacci(n int) int {
    if n <= 1 {
        return n
    }
    return fibonacci(n-1) + fibonacci(n-2)
}

func main() {
    for i := 0; i < 10; i++ {
        fmt.Printf("F(%d) = %d\n", i, fibonacci(i))
    }
}
```

## Author Information

Since this post has an author specified in the frontmatter, the author card will be automatically displayed:

## Complex Nested Content

### Nested Lists with Code

1. **Setup Phase**

   - Install dependencies:
     ```bash
     npm install next-mdx-remote gray-matter
     ```
   - Configure Next.js:
     ```javascript
     // next.config.js
     const nextConfig = {
       experimental: {
         mdxRs: true,
       },
     };
     ```

2. **Development Phase**

   - Create MDX files
   - Set up components
   - Test functionality

3. **Deployment Phase**
   - Build the project
   - Deploy to your platform of choice
   - Monitor performance

### Mixed Content Example

<Callout type="info">
  You can mix different types of content seamlessly in MDX:
</Callout>

> "The future belongs to those who learn more skills and combine them in creative ways." - Robert Greene

<Counter initialValue={42} step={1} />

```json filename="package.json"
{
  "name": "mdx-blog",
  "version": "1.0.0",
  "dependencies": {
    "next": "^14.0.0",
    "next-mdx-remote": "^4.4.1",
    "gray-matter": "^4.0.3",
    "prism-react-renderer": "^2.3.0"
  }
}
```
