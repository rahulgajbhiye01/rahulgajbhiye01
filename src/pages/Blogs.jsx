import { BlogsHero } from "@containers";

const Blogs = () => {
  return (
    <section
      id="blogs"
      className="flex h-screen flex-col items-center px-4 py-10 text-center"
    >
      <BlogsHero />
    </section>
  );
};

export default Blogs;
