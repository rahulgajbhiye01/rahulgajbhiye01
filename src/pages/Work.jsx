import { Works } from "@containers";

const Work = () => {
  return (
    <section
      id="work"
      className="flex h-screen flex-col items-center justify-center px-4 py-10 text-center"
    >
      <Works />
    </section>
  );
};

export default Work;
